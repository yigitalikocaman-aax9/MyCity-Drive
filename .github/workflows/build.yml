name: Build Unity iOS
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Unity
        uses: game-ci/unity-builder@v4
        with:
          targetPlatform: iOS
          projectPath: .
          unityVersion: auto
          license: '{"email":"ygtali.kcmn9@gmail.com","Kaptan9090.":"Kaptan9090.","serial":"personal"}'
